-----------------------------------------
-- main setup (not windows compatible)
-----------------------------------------

# 1. Create and activate virtual environment
sudo apt-get install python3-venv    # Install venv if not available
python3 -m venv .venv
source .venv/bin/activate

# 2. Add Microsoft repository for SQL Server (optional, for DB connectivity)
curl https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add -
sudo apt-get update 

# 3. Install ODBC Driver for SQL Server (only if connecting to MS SQL)
sudo ACCEPT_EULA=Y apt-get install msodbcsql17

# 4. Install Python packages
pip install django djangorestframework pyodbc mssql-django django-cors-headers

# 5. ✅ ADD TENSORFLOW FOR AI MODEL TRAINING
pip install tensorflow==2.8.0 protobuf==3.20.3 numpy==1.21.6 --no-cache-dir --force-reinstall

# 6. Optional shorcut for both 4. and 5. 
pip install -r requirements.txt

# 7. Run server 
python manage.py runserver

# 8. TIC TAC TOE -> Train Model
https://6rtfk8-8000.csb.app/train_tictactoe_model
{"status": "success", "message": "Model trained and saved!", "epochs": 10, "dataset_size": 22882, "save_path": "models/tictactoe_tf_model"}

# 9. TIC TAC TOE -> Download Model (alternative #1)
https://6rtfk8-8000.csb.app/download_tictactoe_model

# 10. TIC TAC TOE -> Zip Model     (alternative #2)
cd tictactoe_tf_model
zip -r tictactoe_tf_model.zip tictactoe_tf_model/


# 11. LINEAR REGRESSION
curl -X POST https://6rtfk8-8000.csb.app/predict/ \
     -H "Content-Type: application/json" \
     -d '{"mission_number": 18.0}'