-----------------------------------------
-- main setup (codespaces - ubuntu)
-----------------------------------------

# 1. Create and activate virtual environment
sudo apt-get update
sudo apt-get install -y python3.12-venv
python3.12 -m venv .venv

# 3. Activate the new environment
source .venv/bin/activate
pip install --upgrade pip setuptools wheel

# 2. Descargar e instalar la clave y el repositorio de Microsoft para Ubuntu/Debian
curl https://packages.microsoft.com/keys/microsoft.asc | sudo tee /etc/apt/trusted.gpg.4.d/microsoft.asc

# Añadir el repositorio correcto
curl https://packages.microsoft.com/config/ubuntu/$(lsb_release -rs)/prod.list | sudo tee /etc/apt/sources.list.d/mssql-release.list

# 2. Actualizar los repositorios del sistema
sudo apt-get update

# 3. Instalar el ODBC Driver 18 (y unixodbc)
# Te pedirá aceptar los términos de licencia en la terminal (puedes usar las flechas y Enter)
sudo ACCEPT_EULA=Y apt-get install -y msodbcsql18 unixodbc-dev

# 4. Install Python packages
pip install django djangorestframework pyodbc mssql-django django-cors-headers

# 5. ✅ ADD TENSORFLOW FOR AI MODEL TRAINING
pip install tensorflow

# 6. ✅ static files
pip install whitenoise
python manage.py collectstatic

# 7. Optional shorcut for both 4. and 5. 
pip freeze > requirements.txt
# conflicting versions delete version number

# 8. Run server 
python manage.py runserver

# 8. TIC TAC TOE -> Train Model
https://6rtfk8-8000.csb.app/train_tictactoe_model
https://literate-space-parakeet-gr6qvx47p7v3vwq-8000.app.github.dev/train_tictactoe_model
{"status": "success", "message": "Model trained and saved!", "epochs": 10, "dataset_size": 22882, "save_path": "models/tictactoe_tf_model"}

# 9. TIC TAC TOE -> Download Model (alternative #1)
https://literate-space-parakeet-gr6qvx47p7v3vwq-8000.app.github.dev/download_tictactoe_model
https://6rtfk8-8000.csb.app/download_tictactoe_model

# 10. TIC TAC TOE -> Zip Model     (alternative #2)
cd tictactoe_tf_model
zip -r tictactoe_tf_model.zip tictactoe_tf_model/


# 11. LINEAR REGRESSION
curl -X POST https://6rtfk8-8000.csb.app/predict/ \
     -H "Content-Type: application/json" \
     -d '{"mission_number": 18.0}'

curl -X POST https://laughing-funicular-wjrr7w754g39ggv-8000.app.github.dev/predict/ \
     -H "Content-Type: application/json" \
     -d '{"mission_number": 18.0}'

/////////////////////////////////////////
// DOCKER
/////////////////////////////////////////

--------------------------
--------------------------
-- FIRST BUILD
--------------------------
--------------------------

# INSTALL DOCKER EXTENSION FOR VISUAL STUDIO

-------------------------------------------------------
# TO SAVE DISK SPACE
-------------------------------------------------------

# This removes all stopped containers, unused networks, and dangling images
docker system prune -a --volumes -f

# This clears out hidden build caches (crucial for freeing up space)
docker builder prune -a -f

# ONLY IF VERSION IS STABLE
# rm -rf .venv/

#
pip cache purge

# Find and delete every __pycache__ directory recursively from your project
find . -type d -name "__pycache__" -exec rm -r {} +

#
sudo apt-get clean

m check disk space
df -h . && du -h --max-depth=1 | sort -hr
-------------------------------------------------------


## BUILD FROM GITHUB CODESPACES  --> "Owned by 'aperezNWO'"
0) https://github.com/codespaces

# docker must be installed on desktop. it was from codespaces, only visual studio code extensions was installed
# dot included !!!!
# (time consumming)

1) docker buildx build -t django_setup_linux .

# create a tag previous to publish
3) docker tag django_setup_linux:latest apereznwogmail/django_setup_linux:latest


# list images built
2) docker image ls 

# log into docker for publish
# get PAT from https://app.docker.com/accounts/apereznwogmail/settings/personal-access-tokens
## privileges : read/write/delete
## must return 'Login Succeeded'
4) echo "YOUR_PAT_HERE" | docker login --username apereznwogmail --password-stdin

# publish in docker hub
# publish is time consuming
# https://hub.docker.com/
5) docker push apereznwogmail/django_setup_linux:latest

# publis on render
# URL FOR RENDER : https://dashboard.render.com/

# 6) docker.io/apereznwogmail/django_setup_linux:latest
6) publish as a docker runtime, not as a docker image.

--------------------------
--------------------------
-- PUBLISH CHANGES
--------------------------
--------------------------

## 0. save changes on git.
git add * && git commit -m"fractal changes" && git push && git pull 

## 1. docker build -t my-spring-boot-app .
docker buildx build -t django_setup_linux .

## 2. docker tag my-spring-boot-app yourusername/your-repo:latest
docker tag django_setup_linux:latest apereznwogmail/django_setup_linux:latest

## 3. log into docker for publish
## get/create PAT from https://app.docker.com/accounts/apereznwogmail/settings/personal-access-tokens
## create with read/write scope
echo "YOUR_PAT_HERE" | docker login --username apereznwogmail --password-stdin

## 4. docker push yourusername/your-repo:latest
docker push apereznwogmail/django_setup_linux:latest 

--------------------------
--------------------------
-- DOCUMENTATION
--------------------------
--------------------------

1) AI GUIDE : https://docs.docker.com/get-started/ --> GORDON

2) GUIDES   : https://docs.docker.com/guides/java/

/////////////////////////////////////////
